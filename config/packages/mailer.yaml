framework:
    mailer:
        dsn: '%env(MAILER_DSN)%'

    messenger:
        routing:
            'Symfony\Component\Mailer\Messenger\SendEmailMessage': null  # Envoyer directement sans passer par Messenger
